# Jade 문법 교과서

*AI가 배우고 풀면서 익히는 Jade 문법 · v0.1*

---

## 이 교과서 쓰는 법

각 레슨은 같은 순서로 진행됩니다.

1. **규칙** — 문법 규칙을 배웁니다.
2. **예시** — 올바른用法과 잘못된 용법을 봅니다.
3. **퀴즈** — 문제를 풉니다.
4. **정답** — 맞았는지 확인하고 해설을 읽습니다.

이 문서의 각 퀴즈는 그 자체가 Jade 학습 데이터입니다. 마지막 부록에서 퀴즈를 Jade 형식으로 바꾸는 법도 알려줍니다.

---

## 레슨 1. 헤더

**규칙**
모든 Jade 문서는 버전 헤더로 시작합니다. 형식은 `@jade/버전` 입니다.

**예시**

올바른 예:

    @jade/0.1

잘못된 예:

    jade@0.1      ← @가 앞에 와야 합니다
    @jade         ← 버전이 없습니다

**퀴즈 1**
다음 중 올바른 헤더는 무엇인가요?
① `jade/0.1`  ② `@jade/0.1`  ③ `@0.1jade`

**정답 1**
② `@jade/0.1`. 헤더는 항상 `@`로 시작하고 `@jade/버전` 형식을 지킵니다.

---

## 레슨 2. 역할 태그 (S / U / A)

**규칙**
대화 학습의 기본 단위입니다. 줄 맨 앞에서 시작합니다.
- `S:` 시스템 지시
- `U:` 사용자 발화
- `A:` 어시스턴트 답변

콜론 뒤에는 공백 하나를 둡니다.

**예시**

    S: 너는 한국어 도우미다.
    U: 안녕?
    A: 안녕하세요!

**퀴즈 2**
다음에서 어시스턴트의 답변은 어느 줄인가요?

    S: 번역해줘.
    U: 사과
    A: apple

**정답 2**
`A: apple`. `A:`로 시작하는 줄이 어시스턴트 답변입니다.

---

## 레슨 3. 분류 태그 (@)

**규칙**
데이터의 큰 범주를 정합니다. `@키:값` 형식이며, 보통 헤더 줄에 함께 씁니다.
자주 쓰는 키: `@task`(과제), `@lang`(언어), `@type`(형태).

**예시**

    @jade/0.1 @task:qa @lang:ko @type:chat

**퀴즈 3 (O/X)**
`@task:qa`는 "이 데이터가 qa 과제임을 나타낸다."

**정답 3**
O. `@task`는 과제 종류를, 값 `qa`는 문답형 과제임을 뜻합니다.

---

## 레슨 4. 메타데이터 (~)

**규칙**
데이터의 품질과 이력을 적습니다. `~키:값` 형식입니다.
자주 쓰는 키: `~quality`(품질), `~difficulty`(난이도), `~src`(출처), `~license`(라이선스), `~hash`(해시), `~tokens`(토큰 수), `~ts`(시각).

**예시**

    ~quality:0.95 ~difficulty:2 ~src:"wiki" ~ts:2026-07-23

문자열 값은 따옴표로 감쌉니다. 숫자는 그대로 씁니다.

**퀴즈 4 (오류 수정)**
다음 메타데이터의 잘못된 부분을 고치세요.

    ~quality:wiki ~src:0.95

**정답 4**
값이 서로 바뀌었습니다. 올바르게는:

    ~quality:0.95 ~src:"wiki"

`quality`는 숫자, `src`는 문자열(따옴표)이어야 자연스럽습니다.

---

## 레슨 5. 데이터 타입 (^)

**규칙**
타입은 추론하지 않고 `^`로 명시합니다. 환각을 줄이기 위해서입니다.
종류: `^str`(문자열) `^int`(정수) `^float`(실수) `^bool`(불리언) `^null` `^arr`(배열) `^obj`(객체).

**예시**

    name="홍길동"^str
    age=25^int
    score=3.14^float
    verified=!true^bool

**퀴즈 5**
`age=25`에 알맞은 타입 태그는? ① `^str` ② `^int` ③ `^arr`

**정답 5**
② `^int`. 25는 정수이므로 `age=25^int`로 씁니다.

---

## 레슨 6. 배열과 중첩

**규칙**
배열은 `[ ]`, 객체는 `{ }`로 감싸고 끝에 타입 태그를 붙입니다.

**예시**

    tags=[ko,qa,beginner]^arr
    meta={author:"team",verified:!true}^obj

**퀴즈 6 (생성)**
취미가 "독서"와 "코딩"인 배열을 Jade로 쓰세요.

**정답 6**

    hobby=[독서,코딩]^arr

---

## 레슨 7. 멀티턴 대화

**규칙**
U와 A를 번갈아 여러 줄 쓰면 여러 번 주고받는 대화가 됩니다. S는 보통 맨 앞에 한 번만 옵니다.

**예시**

    S: 너는 한국어 도우미다.
    U: DB가 뭐야?
    A: 데이터베이스의 약자입니다.
    U: 종류도 알려줘.
    A: 관계형과 비관계형이 있습니다.

**퀴즈 7 (추출)**
위 대화에서 사용자가 두 번째로 한 질문은?

**정답 7**
"종류도 알려줘." 두 번째 `U:` 줄이 사용자의 두 번째 발화입니다.

---

## 레슨 8. 종합

**규칙**
지금까지 배운 것을 한 블록에 모두 담을 수 있습니다. 순서는 보통 헤더 → 분류 → 메타데이터 → 대화 본문입니다.

**예시**

    @jade/0.1 @task:qa @lang:ko
    ~quality:0.95 ~src:"wiki" ~tokens:42

    S: 너는 친절한 한국어 AI 도우미다.
    U: DB가 뭐야?
    A: DB는 데이터베이스의 약자로, 데이터를 체계적으로
       저장·검색·관리하는 시스템입니다.

**퀴즈 8 (오류 수정)**
아래 블록에는 잘못된 곳이 한 군데 있습니다. 찾으세요.

    @jade/0.1 @task:qa
    ~quality:wiki

    S: 도우미다.
    U: 안녕?
    A: 안녕하세요!

**정답 8**
`~quality:wiki`가 잘못되었습니다. `quality`는 0.0~1.0 숫자여야 하므로 예: `~quality:0.9`. 출처를 나타내려면 `~src:"wiki"`를 따로 씁니다.

---

## 부록. 퀴즈를 Jade 학습 데이터로 쓰기

이 교과서의 각 퀴즈는 Jade 형식 그대로 학습 데이터가 될 수 있습니다. `@task:quiz`로 표시하면 됩니다.

    @jade/0.1 @task:quiz @lang:ko
    ~difficulty:1

    U: 다음 중 올바른 헤더는? ① jade/0.1 ② @jade/0.1 ③ @0.1jade
    A: ② @jade/0.1. 헤더는 @로 시작하고 @jade/버전 형식을 지킵니다.

이렇게 하면 "AI가 Jade를 배우는 데이터"가 그 자체가 Jade로 written, 자기 자신을 가르치는 교과서가 됩니다.

---

## 맺음말

규칙을 읽고, 예시를 보고, 퀴즈를 풀고, 정답으로 확인한다. 이 네 단계면 AI는 Jade의 문법을 자연스럽게 익힙니다. 더 많은 퀴즈를 추가할수록 학습 효과는 커집니다.
