# Jade vs XML vs JSON: 데이터 포맷 비교 가이드

이 문서는 Jade, XML, JSON 세 가지 데이터 포맷을 직접 비교하여, 왜 Jade가 특정 상황에서 더 나은 선택인지 설명합니다. 각 포맷의 특징을 객관적으로 분석하였으며, 실제 사용 사례를 통해 차이점을 확인할 수 있습니다.

## 1. 같은 데이터를 세 포맷으로 표현하기

다음은 게임 캐릭터 정보를 저장하는 동일한 데이터를 세 가지 포맷으로 작성한 예시입니다.

### XML
    <player>
        <name>Knight</name>
        <level>25</level>
        <isAlive>true</isAlive>
        <nickname></nickname>
        <items>
            <item>Sword</item>
            <item>Potion</item>
        </items>
    </player>

### JSON
    {
        "player": {
            "name": "Knight",
            "level": 25,
            "isAlive": true,
            "nickname": null,
            "items": [
                "Sword",
                "Potion"
            ]
        }
    }

### Jade
    player :
        name : Knight
        level : 25
        <isAlive>/
        <nickname>;
        
        items :
            - Sword
            - Potion
        .
    .

## 2. 핵심 차이점 분석

### 타이핑 효율성
- XML: 시작 태그와 종료 태그를 모두 입력해야 하며, 같은 단어가 반복됩니다. 예: `<name>Knight</name>` → 'name'이 2회, '<', '>', '/' 등 추가 기호 4개 필요.
- JSON: 따옴표(`"`), 콜론(`:`), 쉼표(`,`), 중괄호(`{}`), 대괄호(`[]`) 등 문법 기호가 많습니다. 특히 배열과 객체의 구분이 복잡합니다.
- Jade: 이름과 값을 연결하는 `:` 하나, 목록은 `-`, 블록 끝은 `.`만으로 충분합니다. 불필요한 기호가 제거되어 타이핑량이 최소화됩니다.

### 가독성
- XML: 태그가 깊게 중첩되면 구조 파악이 어려워집니다. 예: `<a><b><c><d>value</d></c></b></a>`처럼 닫는 태그가 멀리 있으면 실수하기 쉽습니다.
- JSON: 인덴트로 구조를 나타내지만, 중괄호와 대괄호가 섞이면 혼동될 수 있습니다. 특히 큰 파일에서는 괄호 매칭 오류가 자주 발생합니다.
- Jade: 들여쓰기와 `.`만으로 계층 구조가 명확하게 드러납니다. 사람이 읽을 때 눈이 자연스럽게 흐르도록 설계되었습니다.

### 특수 값 표현
- XML: 참/거짓/없음도 태그로 표현해야 합니다. 예: `<isAlive>true</isAlive>`, `<nickname></nickname>`
- JSON: `true`, `false`, `null`을 키워드로 사용하지만, 따옴표 없이 써야 하는 규칙이 있어 초보자에게 혼란을 줄 수 있습니다.
- Jade: `<key>/`, `<key>`, `<key>;`라는 단일 패턴으로 모든 경우를 처리합니다. 의미가 직관적이고 기억하기 쉽습니다.

### 배열 표현
- XML: `<items><item>A</item><item>B</item></items>`처럼 항목마다 태그를 반복해야 합니다.
- JSON: `[ "A", "B" ]`처럼 대괄호와 따옴표, 쉼표를 조합해야 하며, 마지막 쉼표는 허용되지 않습니다.
- Jade: `- A`, `- B`처럼 한 줄에 하나씩 적고, 끝에 `.`을 찍으면 됩니다. 실수 없이 빠르게 작성할 수 있습니다.

## 3. 실제 사용 시나리오 비교

### 설정 파일 작성 (예: 게임 옵션)
- XML: 20~30줄 이상이 되면 태그 누락 위험이 높습니다.
- JSON: 숫자와 문자열 구분을 잘 해야 하며, 쉼표 실수로 전체 파일이 파싱 실패할 수 있습니다.
- Jade: 설정 항목을 하나씩 나열하면 끝입니다. `fullscreen : false` 대신 `<fullscreen>`으로 더 간단하게 표현 가능합니다.

### 데이터 저장 (예: 플레이어 세이브)
- XML: 데이터가 많아질수록 파일 크기가 급격히 커지고, 파싱 속도가 느려집니다.
- JSON: 보편적이라 호환성은 좋지만, 중복된 키 이름과 기호로 인해 파일 용량이 증가합니다.
- Jade: 최소한의 문자로 동일한 정보를 전달하므로, 파일 크기가 작고, 사람이 직접 수정하기 쉬워 유지보수 비용이 낮습니다.

## 4. 결론

| 기준 | XML | JSON | Jade |
|------|-----|------|------|
| 타이핑 양 | 매우 많음 | 많음 | 최소 |
| 가독성 | 낮음 (중첩 시) | 중간 | 높음 |
| 학습 곡선 | 높음 | 중간 | 낮음 |
| 컴퓨터 파싱 효율 | 중간 | 높음 | 높음 (문법 단순) |
| 인간 친화성 | 낮음 | 중간 | 높음 |

Jade는 XML의 복잡함과 JSON의 문법적 노이즈를 제거하고, **사람이 직접 작성하고 수정하기 쉬운 형태**를 목표로 설계되었습니다. 특히 게임 개발, 설정 관리, 소규모 데이터 저장 등에서 큰 장점을 발휘합니다.

이 비교를 바탕으로 프로젝트의 목적에 맞는 포맷을 선택하시기 바랍니다.
