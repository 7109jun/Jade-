# Jade의 탄생: XML 태그와의 결별

이 문서는 Jade 데이터 포맷이 왜 존재하며, 어떤 문제를 해결하기 위해 태어났는지를 기록합니다. 단순한 기능 소개가 아닌, 언어 설계자 Jake Jun의 개인적 경험과 철학을 바탕으로 한 탄생 일화입니다.

## 1. 끔찍했던 XML 태그의 기억

Jade의 모든 설계 결정은 단 하나의 감정에서 시작되었습니다. **"XML의 태그(`< >`)는 정말 끔찍하다."**

게임 개발 과정에서 설정 파일, 아이템 데이터, 퀘스트 정보 등을 저장할 때마다 XML을 사용해야 했습니다. `<name>Knight</name>`처럼 같은 단어를 두 번 타이핑해야 했고, 중첩이 깊어질수록 닫는 태그(`</tag>`)가 어디에 위치해야 하는지 눈으로 추적하는 것이 고통스러웠습니다. 실수로 닫는 태그 하나를 누락하면 수백 줄의 파일 전체가 파싱 오류를 일으켰고, 디버깅 시간은 코딩 시간의 세 배 이상 소요되었습니다.

데이터보다 문법 태그가 더 많은 파일, 사람이 읽기 위해 만들어진 것이 아니라 기계가 파싱하기 위해 희생된 가독성. 이것이 XML에 대한 솔직한 평가였습니다.

## 2. JSON의 대안으로서의 한계

XML의 대안으로 JSON을 고했지만, 역시 만족스럽지 않았습니다. 따옴표(`"`), 쉼표(`,`), 중괄호(`{}`), 대괄호(`[]`) 등 문법 기호가 여전히 많았습니다. 특히 배열과 객체가 중첩되면 괄호 매칭을 확인하는 것이 또 다른 부담이 되었습니다.

무엇보다 JSON은 **사람이 직접 수정하기 위한 포맷이 아니었습니다**. IDE의 자동 완성이나 포맷터에 의존하지 않으면, 쉼표 하나 잘못 찍어 전체 파일이 망가지는 구조였습니다. 게임 설정 파일처럼 개발자가 자주 열고 수정해야 하는 용도에는 적합하지 않다고 판단했습니다.

## 3. 키보드 생체인증 연구에서의 깨달음

Jade의 핵심 설계 원칙인 **"타이핑 효율성"** 은 우연히 발견된 것이 아닙니다. 키보드 입력 패턴과 사용자 피로도를 분석하는 생체인증 연구를 진행하던 중, 불필요한 키 스트로크가 누적될 때 인지 부하(Cognitive Load)가 급격히 증가한다는 사실을 확인했습니다.

같은 정보를 전달할 때, 키 입력 횟수가 30% 감소하면 작업 완료 시간이 20% 단축되고, 실수율도 함께 떨어졌습니다. 이 연구 결과를 데이터 포맷 설계에 적용하기로 결심했습니다. **"불필요한 기호를 제거하는 것은 편의 문제가 아니라, 인간 공학적 필수 조건이다."** 라는 결론에 도달한 순간이었습니다.

## 4. 최소한의 문법으로 최대의 명확성

연구 결과를 바탕으로 다음과 같은 설계 목표를 수립했습니다.

-   **태그 완전 제거**: `< >` 쌍을 없애고, 들여쓰기와 단일 기호만으로 계층 구조 표현
-   **닫기 신호 간소화**: 복잡한 종료 태그 대신 점(`.`) 하나로 블록 마감
-   **특수 값 직관화**: true/false/null을 키워드가 아닌 시각적 기호(`/`, `;`, 무표시)로 표현
-   **배열 표현 단순화**: 대괄호와 쉼표 대신 빼기(`-`) 하나로 목록 항목 시작

이 목표들을 충족하면서도, 파서가 안정적으로 동작할 수 있는 엄격한 규범(들여쓰기 4칸 고정, 키 이름 형식 제약 등)을 동시에 정의했습니다. **"간단함"은 "무규칙"이 아니라, "예측 가능한 규칙의 최소화"** 여야 한다는 원칙이었습니다.
