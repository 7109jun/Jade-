# Jade 고급 가이드: 실전 개발자를 위한 엄격 규칙

이 문서는 Jade를 실제 프로젝트에 적용할 때 반드시 숙지해야 하는 상세 규약을 설명합니다. 기본 문법을 익힌 후, 파싱 오류를 방지하고 데이터 무결성을 보장하기 위해 참고하시기 바랍니다.

## 1. 들여쓰기(Indentation) 엄격 규칙

Jade는 들여쓰기를 구조의 핵심으로 사용합니다. 탭(Tab)과 스페이스(Space)를 혼용하거나 일관되지 않게 사용하면 파서가 블록 경계를 잘못 인식할 수 있습니다.

### 권장 규칙
-   **공백 4칸 고정**: 모든 하위 계층은 정확히 4칸의 스페이스로 들여씁니다.
-   **탭 사용 금지**: 에디터 설정에서 'Insert Spaces'를 활성화하고 Tab Size를 4로 설정하세요.
-   **일관성 유지**: 파일 전체에서 동일한 들여쓰기 방식을 사용해야 합니다.

### 잘못된 예시 (파싱 오류 발생 가능)
    player :
      name : Knight    # 2칸 공백 (오류!)
        level : 25     # 4칸 공백
    .

### 올바른 예시
    player :
        name : Knight  # 4칸 공백
        level : 25     # 4칸 공백
    .

## 2. 값(Value) 내 특수 문자 처리

값 부분에 콜론(:), 마침표(.), 빼기(-) 등 Jade 문법 기호가 포함되면 파서가 이를 구조 기호로 오인식할 수 있습니다. 이때는 값을 따옴표로 감싸야 합니다.

### 따옴표 사용 규칙
-   값에 `:`, `.`, `-`, `#`, `<`, `>` 가 포함될 경우 반드시 큰따옴표(") 또는 작은따옴표(')로 감쌉니다.
-   따옴표 자체를 값으로 쓰고 싶으면 백슬래시(\)로 이스케이프합니다.

### 예시
    message : "Hello: World."       # 콜론과 마침표 포함 → 따옴표 필수
    path : "C:\Users\Admin"         # 백슬래시 포함 → 따옴표 필수
    quote : "She said \"Hi\""       # 따옴표 포함 → 백슬래시 이스케이프
    note : Normal text without symbols  # 특수문자 없음 → 따옴표 생략 가능

## 3. 데이터 타입 명시적 구분

Jade는 기본적으로 값을 문자열(String)로 취급합니다. 숫자(Number), 불리언(Boolean), null은 특별한 표기법을 통해 명시해야 합니다.

### 타입별 표기법
| 타입 | 표기법 | 예시 | 비고 |
|------|--------|------|------|
| 문자열 | 따옴표 없음 또는 있음 | `name : Alice` | 기본 타입 |
| 정수 | 따옴표 없이 숫자만 | `level : 25` | 소수점 없으면 정수 |
| 실수 | 소수점 포함 | `price : 9.99` | 소수점 있으면 실수 |
| 불리언 | `<key>/` 또는 `<key>` | `<isAlive>/` | `/` = true, 없음 = false |
| null | `<key>;` | `<nickname>;` | 세미콜론 필수 |

### 주의사항
-   `"25"` (따표 있음)는 문자열 "25"로 처리됩니다. 산술 연산이 필요하다면 따옴표를 제거하세요.
-   `0` 은 유효한 숫자이지만, 빈 값이나 false로 오해하지 않도록 주의하세요.

## 4. 멀티라인 텍스트 상세 규약

멀티라인 텍스트(`: 0`)는 줄바꿈과 공백을 원본 그대로 보존합니다. 이 과정에서 흔히 발생하는 실수를 예방해야 합니다.

### 규칙
-   `: 0` 뒤에는 반드시 줄바꿈이 와야 합니다. 같은 줄에 내용을 적으면 파싱 오류가 발생합니다.
-   멀티라인 내부의 각 줄은 부모 키보다 **최소 4칸 이상** 들여써야 합니다.
-   멀티라인 블록은 반드시 `.` 으로 마감해야 합니다.
-   멀티라인 내부의 앞뒤 공백(Leading/Trailing whitespace)은 보존되므로, 의도하지 않은 공백이 포함되지 않도록 주의하세요.

### 예시
    description : 0
        This is a multi-line
        text block. Leading spaces
            are preserved exactly.
    .

## 5. 테이블(@) 구문 심화

테이블 구문은 CSV 형식의 데이터를 Jade 내에 임베드할 때 사용합니다. 열(Column) 이름과 행(Row) 데이터의 개수가 일치하지 않으면 파싱 실패 또는 데이터 손실이 발생합니다.

### 규칙
-   열 이름 행(`@table_name : col1,col2,...`)과 데이터 행의 쉼표(,) 개수가 반드시 동일해야 합니다.
-   빈 셀은 물음표(`?`) 또는 빈 값으로 표현합니다.
-   테이블 블록도 `.` 으로 마감해야 합니다.
-   열 이름에 공백이나 특수문자가 포함되면 따옴표로 감싸세요.

### 예시
    @items : name,price,category
    Sword,100,weapon
    Potion,50,consumable
    Shield,,armor          # price가 비어있음 → null 처리
    "Magic Staff",300,"rare item"  # 특수문자 포함 열/값 → 따옴표
    .

## 6. 주석(Comment) 배치 규칙

주석(`#`)은 파서가 완전히 무시하지만, 배치 위치에 따라 가독성과 유지보수성이 달라집니다.

### 권장 배치
-   **블록 상단 주석**: 객체나 배열 블록 시작 전에 해당 블록의 용도를 설명합니다.
-   **인라인 주석**: 값 뒤에 `#` 을 붙여 해당 필드의 의미를 간단히 설명합니다. 값과 `#` 사이에 공백을 2칸 이상 두세요.
-   **파일 헤더 주석**: 파일 최상단에 작성자, 버전, 수정 일자 등을 기록합니다.

### 예시
    # Player Save Data v2.1
    # Last modified: 2026-07-22
    
    player :
        name : Knight      # Character display name
        level : 25         # Current experience level
        
        # Inventory items (max 20 slots)
        items :
            - Sword
            - Potion
        .
    .

## 7. 자주 발생하는 파싱 오류 및 해결법

| 오류 메시지 | 원인 | 해결법 |
|-------------|------|--------|
| Unexpected token '.' | 블록 마감 `.` 이 예상되지 않는 위치에 있음 | 들여쓰기 레벨 확인, 불필요한 `.` 삭제 |
| Unterminated multiline | 멀티라인 텍스트가 `.` 으로 마감되지 않음 | 멀티라인 블록 끝에 `.` 추가 |
| Invalid boolean syntax | `<key>` 뒤에 `/` 나 `;` 이외의 문자가 있음 | `<key>/`, `<key>`, `<key>;` 형식 준수 |
| Table column mismatch | 테이블 행의 쉼표 개수가 열 이름과 다름 | 행 데이터의 쉼표 개수 확인 및 수정 |
| Mixed indentation | 탭과 스페이스가 혼용됨 | 에디터에서 'Convert Indentation to Spaces' 실행 |

## 8. 검증 도구 활용

작성한 Jade 파일이 규약을 준수하는지 확인하려면 다음 방법을 사용하세요.

1.  **converter.html 사용**: 파일을 입력하여 JSON 변환이 성공하면 문법 오류가 없습니다.
2.  **예제 파일 비교**: `examples/` 폴더의 샘플 파일과 들여쓰기, 기호 사용 방식을 대조하세요.
3.  **점진적 테스트**: 복잡한 파일을 한 번에 작성하지 말고, 작은 블록부터 추가하며 변환을 반복 검증하세요.

이 고급 가이드의 규칙을 숙지하면, Jade를 프로덕션 환경에서 안정적으로 사용할 수 있습니다. 추가적인 질문이나 버그 리포트는 GitHub Issues를 통해 제출해 주세요.
