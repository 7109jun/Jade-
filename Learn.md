# Jade 배우기: 데이터 기록의 시작

이 문서는 컴퓨터에 정보를 어떻게 정리해서 저장하는지 처음 접하는 분들을 위해 작성되었습니다. XML, JSON, CSV 같은 전문 용어를 몰라도 전혀 상관없습니다. 우리가 일상에서 쓰는 '목록'이나 '설정'을 컴퓨터가 이해할 수 있게 적는 법을 배워보겠습니다.

## 1. 왜 Jade를 사용할까요?

컴퓨터 프로그램은 사람처럼 말을 이해하지 못합니다. 그래서 사람이 쓴 글자를 특별한 규칙에 맞게 정리해야만 컴퓨터가 그 내용을 읽을 수 있습니다.

예전에 쓰던 방식들은 규칙이 너무 복잡했습니다. 같은 단어를 여러 번 반복해서 써야 하거나, 눈에 보이지 않는 기호들을 엄격하게 지켜야 했습니다. 실수 하나면 전체 내용이 망가지기도 했죠.

Jade는 이 복잡한 규칙들을 모두 없앴습니다. 사람이 글을 쓰듯이 자연스럽게 적으면, 그것이 그대로 컴퓨터의 데이터가 됩니다. 타이핑 양이 적고, 눈으로 봤을 때 구조가 한눈에 들어오도록 설계되었습니다.

## 2. 가장 기본: 이름과 값 쌍 만들기

Jade의 가장 핵심적인 규칙은 단 하나입니다. **왼쪽에 이름을 쓰고, 콜론(:)을 찍고, 오른쪽에 값을 니다.**

예시:
    name : Alice
    age : 13
    job : Student

- `name`, `age`, `job`은 이 정보가 무엇인지 나타내는 **이름(Key)** 입니다.
- `Alice`, `13`, `Student`는 실제 들어가는 **값(Value)** 입니다.
- 중간에 있는 `:`은 이름과 값을 연결한다는 뜻입니다.

이것만으로 컴퓨터는 "이름은 Alice이고, 나이는 13살이며, 직업은 학생이다"라는 사실을 정확히 이해합니다.

## 3. 정보 묶어두기: 객체(Object)

관련된 정보들을 하나의 그룹으로 묶고 싶을 때가 있습니다. 예를 들어 '플레이어'라는 카테고리 안에 이름, 체력, 레벨 등을 넣고 싶다면 어떻게 할까요?

Jade에서는 **이름 뒤에 콜론만 쓰고 다음 줄부터 들여쓰기(탭 또는 공백 4칸)를 해서 하위 정보를 적습니다.** 그리고 모든 하위 정보를 다 썼다면 **마침표(.) 하나로 그룹의 끝을 알립니다.**

예시:
    player :
        name : Knight
        hp : 100
        level : 20
    .

- `player :` 아래에 있는 세 줄은 모두 `player`라는 그룹에 속해 있습니다.
- 마지막 줄의 `.`은 "더 이상 player 그룹에 속한 정보는 없다"는 신호입니다.
- 이렇게 하면 여러 플레이어 데이터를 만들 때도 서로 섞이지 않고 깔끔하게 관리할 수 있습니다.

여러 개의 그룹을 만들 수도 있습니다:

    player :
        name : Knight
        hp : 100
    .
    
    enemy :
        name : Dragon
        hp : 500
    .

## 4. 목록 만들기: 배열(Array)

같은 종류의 항목을 여러 개 나열하고 싶을 때는 **빼기 기호(-)** 를 사용합니다.

예시:
    items :
        - Sword
        - Shield
        - Potion
    .

- `items :` 아래에 `- `로 시작하는 줄들이 목록의 각 항목입니다.
- 마지막 `.`으로 목록의 끝을 알립니다.
- 숫자, 문자, 심지어 다른 그룹도 목록의 항목이 될 수 있습니다.

## 5. 참/거짓, 없음 표현하기

프로그램 설정에서는 "켜짐/꺼짐"이나 "값이 없음"을 자주 표현해야 합니다. Jade는 이를 직관적인 기호로 처리합니다.

| 의미 |写法 | 예시 | 설명 |
|------|-----|------|------|
| 참(True) | `<키>/` | `<fullscreen>/` | 기능이 켜져 있음 |
| 거짓(False) | `<키>` | `<fullscreen>` | 기능이 꺼져 있음 |
| 없음(Null) | `<키>;` | `<nickname>;` | 값이 비어 있음 |

- 꺾쇠 괄호 `< >` 안에 이름을 넣고, 뒤에 `/`, `;` 또는 아무것도 붙이지 않습니다.
- 이 방식은 XML의 `<tag>true</tag>` 같은 복잡한 태그를 완전히 대체합니다.

## 6. 주석 달기

코드나 데이터 파일에 메모를 남기고 싶을 때는 **샵(#) 기호**를 사용합니다.

예시:
    # 이것은 플레이어 기본 설정입니다
    name : Alice      # 플레이어 이름
    level : 10        # 시작 레벨

- `#` 이후의 내용은 컴퓨터가 무시하므로, 사람을 위한 설명으로만 사용됩니다.
- 줄 전체를 주석으로 만들 수도 있고, 코드 옆에 덧붙일 수도 있습니다.

## 7. 실전 예제: 게임 캐릭터 데이터

지금까지 배운 것을 모두 조합해 보면 다음과 같은 완성된 데이터 파일이 됩니다.

    # 캐릭터 프로필
    character :
        name : Hero
        level : 25
        
        stats :
            strength : 15
            agility : 12
            intelligence : 8
        .
        
        inventory :
            - Iron Sword
            - Health Potion
            - Map Fragment
        .
        
        <isAlive>/
        <hasPet>;
    .

이 파일을 읽는 사람은 들여쓰기와 기호만으로 어떤 정보가 어디에 속하는지 3초 만에 파악할 수 있습니다. 이것이 Jade가 추구하는 목표입니다.

## 8. 다음 단계

이제 기본 문법을 익혔으니, 직접 자신의 프로젝트에 적용해 보세요.

- 게임 설정 파일을 Jade로 만들어보기
- 기존에 쓰던 복잡한 설정 파일을 Jade로 변환해 보기
- 친구에게 Jade 문법을 알려주기

궁금한 점이 있거나 더 advanced한 기능(멀티라인 텍스트, 테이블 형식 등)을 알고 싶다면 공식 문서나 커뮤니티를 참고하세요.

데이터 기록은 어렵지 않습니다. Jade와 함께라면 더욱 간단하고 즐겁게 할 수 있습니다.
