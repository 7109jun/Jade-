# Jade 엄격 규범: 언어 명세 준수 가이드

이 문서는 Jade 데이터 포맷을 사용할 때 반드시 준수해야 하는 언어적 규범(Normative Rules)을 정의합니다. 여기서 설명하는 규칙은 권고사항이 아닌, **언어의 정의로서 강제되는 제약**입니다. 이를 위반할 경우 파서의 동작은 정의되지 않으며(Undefined Behavior), 데이터 손실이나 예기치 않은 결과가 발생할 수 있습니다.

## 1. 들여쓰기의 의미론적 엄격성

Jade에서 들여쓰기는 가독성을 위한 장식이 아닙니다. **들여쓰기 수준(Indentation Level) 자체가 구문 구조(Syntax Structure)를 정의합니다.**

### 1.1 들여쓰기 단위의 불변성
-   모든 들여쓰기는 **정확히 4개의 스페이스 문자**여야 합니다.
-   2칸, 3칸, 5칸 등의 변형은 허용되지 않습니다. 이는 문법 오류(Syntax Error)로 처리됩니다.
-   탭(Tab) 문자는 공백으로 변환되기 전에도 큰화 단계에서 거부됩니다. 에디터 설정과 무관하게 소스 코드 내에 탭 문자가 존재해서는 안 됩니다.

### 1.2 들여쓰기 일관성의 절대성
-   동일 계층(Hierarchy Level) 내의 모든 형제 노드(Sibling Nodes)는 **비트 단위(Bit-level)로 동일한 들여쓰기**를 가져야 합니다.
-   시각적으로 비슷해 보여도 공백 개수가 1개라도 다르면 다른 계층으로 인식되거나 파싱 실패가 발생합니다.
-   파일 전체에 걸쳐 들여쓰기 단위가 변경되어서는 안 됩니다. 첫 줄부터 마지막 줄까지 4칸 페이스 규칙이 유지되어야 합니다.

### 1.3 빈 줄과 들여쓰기
-   빈 줄(Empty Line)은 들여쓰기 수준을 리셋하지 않습니다.
-   그러나 빈 줄 이후에 오는 콘텐츠의 들여쓰기는 **직전 유효 노드의 부모 블록 기준**으로 계산됩니다.
-   빈 줄에 공백을 넣는 것은 권장되지 않습니다. 공백이 포함된 빈 줄은 해당 공백이 들여쓰기로 오인식될 수 있어 파서 혼란을 유발합니다.

## 2. 키(Key) 이름의 형식적 제약

키 이름은 단순한 식별자가 아닙니다. 파서가 구조를 판단하는 핵심 토큰으로서 엄격한 형식을 따릅니다.

### 2.1 허용 문자 집합
-   키 이름은 `[a-zA-Z_][a-zA-Z0-9_-]*` 정규식을 완전히 만족해야 합니다.
-   숫자로 시작하는 키(`2name`), 특수문자 포함 키(`user-name!`, `data.value`)는 **문법 오류**입니다.
-   공백이 포함된 키(`my name`)는 두 개의 별도 토으로 분리되어 파싱 논리가 붕괴됩니다.

### 2.2 예약어 및 충돌 방지
-   현재 Jade 명세에서 예약어(Reserved Word)는 없습니다.
-   그러나 `<`, `>`, `.`, `-`, `:`, `#`, `@`, `/`, `;` 등 문법 기호와 동일한 문자열을 키 이름으로 사용하는 것은 **미래 호환성(Future Compatibility)을 위해 금지**됩니다.
-   파서 업데이트 시 해당 문자열이 새로운 문법 요소로 채택될 경우, 기존 파일이 손상될 수 있습니다.

### 2.3 대소문자 민감성(Case Sensitivity)
-   키 이름은 **대소문자를 구분**합니다.
-   `Name`, `name`, `NAME`은 서로 다른 키로 취급됩니다.
-   의도하지 않은 대소문자 혼용은 데이터 중복 또는 누락으로 이어집니다. 프로젝트 내에서 키 네이밍 컨벤션(예: snake_case)을 엄격히 통일해야 합니다.

## 3. 값(Value)의 타입 추론 규칙

Jade는 명시적 타입 선언(Type Annotation)을 지원하지 않습니다. 값의 타입은 **리터럴 형태(Literal Form)에 의해 자동으로 추론**되며, 이 추론 규칙은 변경 불가능합니다.

### 3.1 문자열(String) 추론 조건
-   따옴표가 없는 값 중, 다음 패턴에 **완전히 일치하지 않는** 모든 값은 문자열로 처리됩니다.
    -   정수 패턴: `^-?[0-9]+$`
    -   실수 패턴: `^-?[0-9]+\.[0-9]+$`
    -   불리언/널 패턴: `<...>/`, `<...>`, `<...>;`
-   `"123"` (따옴표 있음)은 항상 문자열입니다. 산술 연산 대상이라면 따옴표를 제거해야 합니다.
-   `true`, `false`, `null` 키워드는 **문자열로 처리**됩니다. 불리언/널은 반드시 `<key>/`, `<key>`, `<key>;` 형식을 사용해야 합니다.

### 3.2 숫자(Number) 추론의 비가역성
-   `010` 은 십진수 10으로 처리됩니다. 8진수 표기는 지원하지 않습니다.
-   과학적 표기법(Scientific Notation, 예: `1e5`)은 **지원하지 않습니다**. 문자열로 처리됩니다.
-   부동소수점 정밀도(Floating-point Precision)는 파서가 아닌 JSON 직렬화(JSON Serialization) 단계에서 결정됩니다. Jade 레벨에서는 원본 리터럴을 보존하지만, 변환 후 값이 미세하게 달라질 수 있음을 인지해야 합니다.

### 3.3 암묵적 타입 캐스팅의 금지
-   Jade 파서는 **자동 타입 캐스팅(Implicit Type Casting)을 수행하지 않습니다**.
-   `"100"` (문자열) 과 `100` (정수) 은 서로 다른 값입니다. 비교, 연산, 병합 시 타입 불일치 오류가 발생할 수 있습니다.
-   외부 시스템과의 연동 시에는 반드시 타입 검증을 거친 후 사용해야 합니다.

## 4. 블록 구조의 종료 조건 엄격성

블록(Object, Array, Multiline Text, Table)의 종료는 **유일하고 명확한 신호**에 의해서만 이루어집니다.

### 4.1 마침표(.)의 독점적 역할
-   블록 종료를 나타내는 `.` 은 **해당 블록의 들여쓰기 수준과 정확히 일치하는 위치**에만 올 수 있습니다.
-   더 깊은 들여쓰기의 `.` 은 하위 블록의 종료로 해석됩니다.
-   더 얕은 들여쓰기의 `.` 은 상위 블록의 종료로 해석되며, 현재 블록은 **종료되지 않은 채 방치**되어 파싱 오류가 발생합니다.

### 4.2 암묵적 종료의 부재
-   파일 끝(EOF)은 블록을 자동으로 종료하지 않습니다.
-   모든 열린 블록은 **명시적인 `.` 으로 마감**되어야 합니다.
-   마지막 블록의 `.` 을 생략하면 "Unterminated Block" 오류가 발생합니다. 이는 설계상의 의도이며, 자동 완성에 의존해서는 안 됩니다.

### 4.3 중첩 블록의 순차적 종료
-   중첩된 블록은 **안쪽에서 바깥쪽으로 순차적으로** 종료되어야 합니다.
-   바깥 블록을 먼저 종료하려 하면(`.` 을过早에 사용하면), 안쪽 블록은 유효 범위를 벗어나게 되어 파싱 실패가 발생합니다.
-   각 `.` 은 **직전에 열린 가장 안쪽 블록**만을 종료합니다.

## 5. 특수 값 표현의 완전 열거(Closed Enum)

불리언과 null 값은 `<key>` 기반의 **폐쇄 집합(Closed Set)** 으로만 표현됩니다.

### 5.1 허용 형식의 완전 목록
-   True: `<identifier>/` (슬래시 필수, 공백 없음)
-   False: `<identifier>` (아무것도 붙이지 않음)
-   Null: `<identifier>;` (세미콜론 필수, 공백 없음)

### 5.2 비허용 형식 및 결과
-   `<identifier> /` (공백 포함): 파서는 `<identifier>` 를 false로, `/` 를 별도의 토큰으로 인식하여 구문 오류 발생
-   `<identifier>true</identifier>`: XML 스타일은 **지원하지 않습니다**. 전체가 문자열로 처리되거나 오류 발생
-   `true`, `false`, `null` 키워드: 문자열 리터럴로 처리됨 (3.1 참조)
-   `<identifier>?`, `<identifier>!` 등: 정의되지 않은 수식어로, 파서 버전에 따라 오류 또는 무시 처리

### 5.3 식별자 내부 공백의 금지
-   `<is Alive>` 는 **두 개의 토**(`<is` 와 `Alive>`)으로 분리됩니다.
-   쇠 괄호 내부에는 **공백이 허용되지 않습니다**. 복합 개념은 언더스코어(`is_alive`) 또는 카멜케이스(`isAlive`)로 표현해야 합니다.

## 6. 주석의 비간섭성(Non-interference) 원칙

주석(`#`)은 파싱 과정에서 **완전히 제거**되지만, 그 위치가 구문 분석에 영향을 미칠 수 있습니다.

### 6.1 주석의 토큰화 우선순위
-   `#` 이후의 내용은 **동일 줄 내에서만** 주석으로 처리됩니다.
-   줄바꿈 이후에는 주석이 종료되며, 다음 줄은 새로운 구문으로 해석됩니다.
-   멀티라인 주석(Multi-line Comment)은 **지원하지 않습니다**. 여러 줄에 걸친 설명은 각 줄마다 `#` 을 반복해야 합니다.

### 6.2 인라인 주석의 위치 제약
-   인라인 주석은 **값(Value) 큰이 완전히 종료된 후에만** 올 수 있습니다.
-   `name : Alice # comment` → 정상 (Alice가 문자열 토큰으로 완료됨)
-   `items : # comment` → **오류** (`:` 뒤에 값이 없으므로 객체/배열 시작으로 해석되려다 주석이 방해함)
-   값이 없는 키(객체/배열 시작)에는 인라인 주석을 붙일 수 없습니다. 주석은 별도 줄에 작성해야 합니다.

### 6.3 주석과 들여쓰기의 독립성
-   주석 줄의 들여쓰기는 **구문 구조에 영향을 주지 않습니다**.
-   그러나 주석의 들여쓰기가 주변 코드와 크게 다르다면, 유지보수 시 혼란을 야기할 수 있으므로 **논리적 계층에 맞춰 들여쓰기를 맞추는 것**이 규범입니다.

## 7. 규범 위반 시의 책임

이 문서에 명시된 규범을 위반하여 발생한 문제에 대해 Jade 명세는 어떠한 보증도 제공하지 않습니다.

-   **파서 버전 간 동작 차이**: 규범을 위반한 파일은 파서 버전에 따라 다르게 해석될 수 있습니다.
-   **데이터 손실**: Undefined Behavior 영역에서의 파싱 결과는 예측할 수 없으며, 일부 정보가 silently drop될 수 있습니다.
-   **호환성断裂**: 규범 준수 파일만이 모든 Jade 구현체(Jade Protocol Engine, 타사 파서 등)에서 동일하게 동작함이 보장됩니다.

프로덕션 환경에서는 **반드시 이 규범을 준수**하고, CI/CD 파이프라인에 Jade 린터(Linter) 또는 검증 크립트를 통합하여 규범 위반을 사전에 차단하시기 바랍니다.
